<h1>Listing ips</h1>

<table>
  <tr>
    <th>Department</th>
    <th>IPs (with child's)</th>
  </tr>

<% @deps.each do |d| %>
  <tr>
    <td><%= d.name %></td>
    <td>
	  <%= render :partial => "department_ips", :locals =>{:department => d} %>
	</td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New IP', new_ip_path %>
